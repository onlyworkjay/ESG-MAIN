<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.esg.favorite.dao.FavoriteDao">

    <!-- 담당자: 장지혁 -->

    <!-- 즐겨찾기 목록 조회 -->
    <select id="getFavorites" resultType="com.project.esg.favorite.vo.Favorite">
        SELECT user_id, product_id, created_at, description
        FROM product_favorites
        WHERE user_id = #{userId}
    </select>

    <!-- 즐겨찾기 여부 확인 -->
    <select id="checkFavorite" resultType="int">
        SELECT COUNT(*)
        FROM product_favorites
        WHERE user_id = #{userId}
          AND product_id = #{productId}
    </select>

    <!-- 즐겨찾기 등록 -->
    <insert id="addFavorite">
        INSERT INTO product_favorites (user_id, product_id, created_at, description)
        VALUES (#{userId}, #{productId}, CONVERT_TZ(NOW(), '+00:00', '+09:00'), #{description})
    </insert>

    <!-- 즐겨찾기 취소 -->
    <delete id="deleteFavorite">
        DELETE FROM product_favorites
        WHERE user_id = #{userId}
          AND product_id = #{productId}
    </delete>

</mapper>